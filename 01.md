# Agent 执行规则摘要

为避免新对话中行为跑偏，建议在 Agent 提示词中确保以下约束被保留：

1. 不默认向用户索要 section_id
2. 必须自动查数据库定位目标节
3. 必须读取该节及直属子节内容
4. 必须先检查已有数据再决定新增/补全
5. 选择题不能被限制为单选题，允许多选题
6. 多选题沿用 `choice` 类型，用题干、答案格式、解析区分
7. 必须写库后再做验证
8. 最终必须向用户回报新增结果

---

## 数据库结构备忘

### books
- id
- title
- short_name
- sort_order

### sections
- id
- book_id
- parent_id
- heading_level
- heading_text
- content
- sort_order

### study_points
- id
- section_id
- point_text
- detail
- sort_order
- importance

### exam_questions
- id
- section_id
- point_id
- question_type
- question_text
- options
- answer
- explanation
- sort_order

---

## 接口备忘

### 查询接口
```text
GET /api/section/{section_id}
GET /api/section/{section_id}/children
GET /api/section/{section_id}/points
GET /api/section/{section_id}/questions
GET /api/books/progress
```

### 写入接口
```text
POST /api/point
POST /api/question
```

---

## 题型落库约定

### 单选题
- question_type: `choice`
- question_text 示例：`【单选题】下列关于观察法的说法，正确的是（ ）。`
- answer 示例：`B`

### 多选题
- question_type: `choice`
- question_text 示例：`【多选题】下列属于实验法特点的有（ ）。`
- answer 示例：`A,B,D`

### 填空题
- question_type: `fill_blank`
- question_text 示例：`心理测验的两个基本要求是____和____。`
- answer 示例：`信度；效度`

---
